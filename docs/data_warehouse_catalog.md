# Data warehouse catalog

> Generated by `make catalog`. Do not edit: the descriptions come from the dbt
> `schema.yml` files, the types and the column order from the lake. Row counts are
> deliberately absent -- `make shape` prints those, and they go stale in a document.

## Published surface (`marts`)

One row per sales order line, every dimension flattened on.

### `obt_sales_order_line`

One row per sales order line, at the same grain as fct_sales_order_line, with every dimension flattened on. This is the published surface of the warehouse, so its column list is an enforced contract: an upstream change that would alter it fails the build. A column's prefix names the dimension or the role it came from -- `customer_` the ordering customer, `order_date_` and `expected_delivery_date_` two role-plays of dim_date. Foreign keys are absent on purpose: the attribute is already here.

| Column | Type | Tests | Description |
|---|---|---|---|
| `sales_order_line_key` | bigint | `not_null`, `unique` | The grain. Carried through from the fact unchanged. |
| `sales_order_key` | bigint |  | The order this line belongs to. Degenerate: there is no order dimension. |
| `quantity` | bigint |  | Units ordered. |
| `unit_price` | decimal(18,2) |  | Price charged per unit on this line. |
| `tax_rate` | decimal(18,3) |  | Tax rate applied to this line, as a percentage. |
| `picked_quantity` | bigint |  | Units actually picked, which may be short of `quantity`. |
| `backorder_order_key` | bigint |  | Order this one was backordered from, or NULL. Degenerate. |
| `is_undersupply_backordered` | boolean |  | Whether a short pick becomes a backorder rather than a cancellation. |
| `sales_order_line_processed_at` | timestamp with time zone |  | When the load that wrote this row ran. Constant for a given load. |
| `customer_customer_name` | varchar |  | Ordering customer — The customer's trading name. |
| `customer_customer_category_name` | varchar |  | Ordering customer — Customer category. |
| `customer_buying_group_name` | varchar |  | Ordering customer — Buying group, or NULL when the customer is in none. |
| `customer_primary_contact_full_name` | varchar |  | Ordering customer — The person's full name as the source holds it. Primary contact. |
| `customer_primary_contact_is_system_user` | boolean |  | Ordering customer — Whether the primary contact has a login in the source system. |
| `customer_primary_contact_is_employee` | boolean |  | Ordering customer — Whether the primary contact is a Wide World Importers employee. |
| `customer_primary_contact_is_salesperson` | boolean |  | Ordering customer — Whether the primary contact is a salesperson. |
| `customer_primary_contact_phone_number` | varchar |  | Ordering customer — Primary contact's phone number. |
| `customer_primary_contact_email_address` | varchar |  | Ordering customer — Primary contact's email address. |
| `customer_alternate_contact_full_name` | varchar |  | Ordering customer — The person's full name as the source holds it. Alternate contact, or NULL. |
| `customer_delivery_method_name` | varchar |  | Ordering customer — How goods reach this customer. |
| `customer_delivery_city_name` | varchar |  | Ordering customer — Delivery city name. |
| `customer_delivery_state_province_name` | varchar |  | Ordering customer — State or province of the delivery city. |
| `customer_delivery_state_province_population` | bigint |  | Ordering customer — Latest recorded population of that state or province. |
| `customer_delivery_country_name` | varchar |  | Ordering customer — Country of the delivery city. |
| `customer_delivery_country_formal_name` | varchar |  | Ordering customer — That country's formal name. |
| `customer_delivery_country_type` | varchar |  | Ordering customer — What kind of entity the country is. |
| `customer_delivery_country_population` | bigint |  | Ordering customer — Latest recorded population of that country. |
| `customer_credit_limit` | decimal(18,2) |  | Ordering customer — Credit limit, or NULL for no limit. |
| `customer_standard_discount_percentage` | decimal(18,3) |  | Ordering customer — Standard discount applied to this customer's lines. |
| `customer_is_on_credit_hold` | boolean |  | Ordering customer — Whether the customer is currently on credit hold. |
| `customer_payment_term_days` | bigint |  | Ordering customer — Days the customer has to pay. |
| `customer_phone_number` | varchar |  | Ordering customer — Customer's main phone number. |
| `customer_website_url` | varchar |  | Ordering customer — Customer's website. |
| `customer_delivery_address` | varchar |  | Ordering customer — Delivery street address, both source lines joined and trimmed. |
| `customer_delivery_postal_code` | varchar |  | Ordering customer — Delivery postal code. |
| `customer_postal_address` | varchar |  | Ordering customer — Postal street address, both source lines joined and trimmed. |
| `customer_postal_postal_code` | varchar |  | Ordering customer — Postal code of the postal address. |
| `bill_to_customer_name` | varchar |  | Bill-to customer — The customer's trading name. |
| `salesperson_person_full_name` | varchar |  | Salesperson — The person's full name as the source holds it. |
| `picked_by_person_person_full_name` | varchar |  | Picker — The person's full name as the source holds it. |
| `contact_person_person_full_name` | varchar |  | Customer contact — The person's full name as the source holds it. |
| `stock_item_name` | varchar |  | Stock item — Product name. |
| `color_name` | varchar |  | Stock item — Colour of the item, or NULL. |
| `supplier_name` | varchar |  | Stock item — Supplier's trading name. |
| `is_chiller_stock` | boolean |  | Stock item — Whether the item must be kept refrigerated. |
| `package_type_package_type_name` | varchar |  | Package type — name of this line's packaging. |
| `order_date_full_date` | date |  | Order date — The calendar date itself. |
| `expected_delivery_date_full_date` | date |  | Expected delivery date — The calendar date itself. |
| `order_date_year` | bigint |  | Order date — Calendar year. |
| `expected_delivery_date_year` | bigint |  | Expected delivery date — Calendar year. |
| `order_date_year_week` | bigint |  | Order date — ISO year and week as `yyyyww`. The ISO year, so the last days of December can belong to the next one. |
| `expected_delivery_date_year_week` | bigint |  | Expected delivery date — ISO year and week as `yyyyww`. The ISO year, so the last days of December can belong to the next one. |
| `order_date_year_day` | bigint |  | Order date — Calendar year and day-of-year as `yyyyddd`. |
| `expected_delivery_date_year_day` | bigint |  | Expected delivery date — Calendar year and day-of-year as `yyyyddd`. |
| `order_date_fiscal_year` | bigint |  | Order date — Fiscal year. Wide World Importers starts its year in April, so April 2024 falls in FY2025. |
| `expected_delivery_date_fiscal_year` | bigint |  | Expected delivery date — Fiscal year. Wide World Importers starts its year in April, so April 2024 falls in FY2025. |
| `order_date_fiscal_qtr` | varchar |  | Order date — Fiscal quarter as `Q1`–`Q4`, counted from April. |
| `expected_delivery_date_fiscal_qtr` | varchar |  | Expected delivery date — Fiscal quarter as `Q1`–`Q4`, counted from April. |
| `order_date_month` | bigint |  | Order date — Calendar month, 1–12. |
| `expected_delivery_date_month` | bigint |  | Expected delivery date — Calendar month, 1–12. |
| `order_date_month_name` | varchar |  | Order date — Month name in English. |
| `expected_delivery_date_month_name` | varchar |  | Expected delivery date — Month name in English. |
| `order_date_week_day` | bigint |  | Order date — Day of week, 1 = Sunday through 7 = Saturday. |
| `expected_delivery_date_week_day` | bigint |  | Expected delivery date — Day of week, 1 = Sunday through 7 = Saturday. |
| `order_date_day_name` | varchar |  | Order date — Day name in English. |
| `expected_delivery_date_day_name` | varchar |  | Expected delivery date — Day name in English. |
| `order_date_day_is_weekday` | integer |  | Order date — 1 on Monday–Friday, 0 on Saturday and Sunday. |
| `expected_delivery_date_day_is_weekday` | integer |  | Expected delivery date — 1 on Monday–Friday, 0 on Saturday and Sunday. |
| `sales_order_picking_completed_date_full_date` | date |  | Order picking-completed date — The calendar date itself. |
| `sales_order_line_picking_completed_date_full_date` | date |  | Line picking-completed date — The calendar date itself. |

## Star schema (`core`)

Conformed dimensions and the fact they hang off.

### `dim_customer`

One row per customer. Category, buying group, both contacts, delivery method and the delivery city's whole geography are flattened on, so a consumer never joins back for them.

| Column | Type | Tests | Description |
|---|---|---|---|
| `customer_key` | bigint | `not_null`, `unique` | Natural key from `Sales.Customers`. The grain. |
| `customer_name` | varchar |  | The customer's trading name. |
| `bill_to_customer_key` | bigint | `-> dim_customer.customer_key`, `not_null` | The customer this one is invoiced through; most often itself. A real self-reference, which is why the key stays. |
| `customer_category_name` | varchar |  | Customer category, flattened from `Sales.CustomerCategories`. |
| `buying_group_key` | bigint |  | Buying group the customer belongs to, or NULL. Kept because no dimension addresses `Sales.BuyingGroups`. |
| `buying_group_name` | varchar |  | Buying group name, or NULL when the customer is in none. |
| `primary_contact_full_name` | varchar |  | The person's full name as the source holds it. Primary contact. |
| `primary_contact_is_system_user` | boolean |  | Whether the primary contact has a login in the source system. |
| `primary_contact_is_employee` | boolean |  | Whether the primary contact is a Wide World Importers employee. |
| `primary_contact_is_salesperson` | boolean |  | Whether the primary contact is a salesperson. |
| `primary_contact_phone_number` | varchar |  | Primary contact's phone number. |
| `primary_contact_email_address` | varchar |  | Primary contact's email address. |
| `alternate_contact_full_name` | varchar |  | The person's full name as the source holds it. Alternate contact, or NULL. |
| `delivery_method_name` | varchar |  | How goods reach this customer, flattened from `Application.DeliveryMethods`. |
| `delivery_city_key` | bigint |  | Delivery city. Kept because `Application.Cities` has no dimension of its own. |
| `delivery_city_name` | varchar |  | Delivery city name. |
| `delivery_state_province_name` | varchar |  | State or province the delivery city sits in. |
| `delivery_state_province_population` | bigint |  | Latest recorded population of that state or province. |
| `delivery_country_name` | varchar |  | Country the delivery city sits in. |
| `delivery_country_formal_name` | varchar |  | That country's formal name, e.g. *United States of America*. |
| `delivery_country_type` | varchar |  | What kind of entity the country is, e.g. *Country*, *Territory*. |
| `delivery_country_population` | bigint |  | Latest recorded population of that country. |
| `postal_city_key` | bigint |  | Postal city. Carried without attributes: it has never differed from `delivery_city_key`, and `assert_customer_postal_city_matches_delivery` fails the build the day it does. |
| `credit_limit` | decimal(18,2) |  | Credit limit in the source's currency, or NULL for no limit. |
| `standard_discount_percentage` | decimal(18,3) |  | Standard discount applied to this customer's lines. |
| `is_on_credit_hold` | boolean |  | Whether the customer is currently on credit hold. |
| `payment_term_days` | bigint |  | Days the customer has to pay. |
| `phone_number` | varchar |  | Customer's main phone number. |
| `website_url` | varchar |  | Customer's website. |
| `delivery_address` | varchar |  | Delivery street address, the source's two lines joined and trimmed. NULL when both lines are empty. |
| `delivery_postal_code` | varchar |  | Delivery postal code. |
| `postal_address` | varchar |  | Postal street address, the source's two lines joined and trimmed. NULL when both lines are empty. |
| `postal_postal_code` | varchar |  | Postal code of the postal address. |

### `dim_date`

One row per day from 2000-01-01 to 2050-12-31, generated rather than loaded. Role-played by the fact: order date, expected delivery, and the picking-completed dates.

| Column | Type | Tests | Description |
|---|---|---|---|
| `date_key` | integer | `not_null`, `unique` | `yyyymmdd` as an integer. The grain, and what every date foreign key in the fact holds. |
| `full_date` | date | `not_null`, `unique` | The calendar date itself. |
| `year` | bigint |  | Calendar year. |
| `year_week` | bigint |  | ISO year and week as `yyyyww`. The ISO year, so the last days of December can belong to the next one. |
| `year_day` | bigint |  | Calendar year and day-of-year as `yyyyddd`. |
| `fiscal_year` | bigint |  | Fiscal year. Wide World Importers starts its year in April, so April 2024 falls in FY2025. |
| `fiscal_qtr` | varchar |  | Fiscal quarter as `Q1`–`Q4`, counted from April. |
| `month` | bigint |  | Calendar month, 1–12. |
| `month_name` | varchar |  | Month name in English. |
| `week_day` | bigint |  | Day of week, 1 = Sunday through 7 = Saturday. |
| `day_name` | varchar |  | Day name in English. |
| `day_is_weekday` | integer |  | 1 on Monday–Friday, 0 on Saturday and Sunday. |

### `dim_package_type`

One row per package type. Role-played by the fact for the line's own packaging, and flattened into `dim_stock_item`.

| Column | Type | Tests | Description |
|---|---|---|---|
| `package_type_key` | bigint | `not_null`, `unique` | Natural key from `Warehouse.PackageTypes`. The grain. |
| `package_type_name` | varchar |  | Package type name, e.g. *Each*, *Carton*, *Pallet*. |

### `dim_person`

One row per person: system users, employees and salespeople alike. Role-played by the fact as salesperson, picker and contact.

| Column | Type | Tests | Description |
|---|---|---|---|
| `person_key` | bigint | `not_null`, `unique` | Natural key from `Application.People`. The grain. |
| `person_full_name` | varchar |  | The person's full name as the source holds it. |
| `person_preferred_name` | varchar |  | What the person is called day to day. |
| `is_system_user` | boolean |  | Whether the person has a login in the source system. |
| `is_employee` | boolean |  | Whether the person is a Wide World Importers employee. |
| `is_salesperson` | boolean |  | Whether the person is a salesperson. |
| `phone_number` | varchar |  | The person's phone number. |
| `email_address` | varchar |  | The person's email address. |

### `dim_stock_item`

One row per stock item, with both package types, colour and supplier flattened on. Keyed on the natural key, like every other dimension here.

| Column | Type | Tests | Description |
|---|---|---|---|
| `stock_item_key` | bigint | `not_null`, `unique` | Natural key from `Warehouse.StockItems`. The grain. |
| `stock_item_name` | varchar |  | Product name as the catalogue holds it. |
| `supplier_key` | bigint |  | Supplier of the item. Kept because `Purchasing.Suppliers` has no dimension of its own. |
| `color_key` | bigint |  | Colour of the item, or NULL. Kept for the same reason. |
| `unit_package_type_name` | varchar |  | How a single unit is packaged, e.g. *Each*. |
| `outer_package_type_name` | varchar |  | How units are packaged for shipping, e.g. *Carton*. |
| `brand` | varchar |  | Brand, or NULL for unbranded stock. |
| `size` | varchar |  | Size as free text, e.g. *L*, *500g*. |
| `lead_time_days` | bigint |  | Days the supplier takes to deliver this item. |
| `quantity_per_outer` | bigint |  | Units in one outer package. |
| `is_chiller_stock` | boolean |  | Whether the item must be kept refrigerated. |
| `tax_rate` | decimal(18,3) |  | Tax rate applied to the item, as a percentage. |
| `unit_price` | decimal(18,2) |  | List price for one unit. |
| `recommended_retail_price` | decimal(18,2) |  | Recommended retail price for one unit, or NULL. |
| `typical_weight_per_unit` | decimal(18,3) |  | Typical weight of one unit, in kilograms. |
| `color_name` | varchar |  | Colour name, or NULL. |
| `supplier_name` | varchar |  | Supplier's trading name. |

### `fct_sales_order_line`

One row per sales order line -- the grain the whole star hangs off. Measures and degenerate dimensions, plus a foreign key per dimension role.

| Column | Type | Tests | Description |
|---|---|---|---|
| `sales_order_line_key` | bigint | `not_null`, `unique` | Natural key from `Sales.OrderLines`. The declared grain. |
| `sales_order_key` | bigint | `not_null` | The order this line belongs to. Degenerate: there is no order dimension. |
| `stock_item_key` | bigint | `-> dim_stock_item.stock_item_key`, `not_null` | Item sold. |
| `package_type_key` | bigint | `-> dim_package_type.package_type_key`, `not_null` | How this line was packaged. |
| `quantity` | bigint |  | Units ordered. |
| `unit_price` | decimal(18,2) |  | Price charged per unit on this line, which may differ from the item's list price. |
| `tax_rate` | decimal(18,3) |  | Tax rate applied to this line, as a percentage. |
| `picked_quantity` | bigint |  | Units actually picked, which may be short of `quantity`. |
| `customer_key` | bigint | `-> dim_customer.customer_key`, `not_null` | Customer the order was placed by. |
| `bill_to_customer_key` | bigint | `-> dim_customer.customer_key`, `not_null` | Customer the order is invoiced to. Resolved from the ordering customer, once, on a unique key -- `Sales.Orders` does not carry it. |
| `salesperson_key` | bigint | `-> dim_person.person_key`, `not_null` | Salesperson credited with the order. |
| `picked_by_person_key` | bigint | `-> dim_person.person_key` | Person who picked the line, or NULL while it is unpicked. |
| `contact_person_key` | bigint | `-> dim_person.person_key`, `not_null` | Customer-side contact for the order. |
| `backorder_order_key` | bigint |  | Order this one was backordered from, or NULL. Degenerate. |
| `is_undersupply_backordered` | boolean |  | Whether a short pick becomes a backorder rather than a cancellation. |
| `sales_order_line_processed_at` | timestamp with time zone |  | When the load that wrote this row ran, from dlt's `_dlt_load_id`. Constant for a given load, which is what makes two builds comparable. |
| `order_date_key` | integer | `-> dim_date.date_key`, `not_null` | Date the order was placed. |
| `expected_delivery_date_key` | integer | `-> dim_date.date_key` | Date delivery is expected, or NULL. |
| `sales_order_picking_completed_date_key` | integer | `-> dim_date.date_key` | Date picking finished for the whole order, or NULL. The time of day is dropped here. |
| `sales_order_line_picking_completed_date_key` | integer | `-> dim_date.date_key` | Date picking finished for this line, or NULL. The time of day is dropped here. |

