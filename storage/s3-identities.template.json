{
  "_comment": [
    "Template, not configuration. The placeholders are replaced at container start from",
    "S3_ACCESS_KEY and S3_SECRET_KEY in the environment, so no key is ever committed.",
    "Without an identity file SeaweedFS answers 403 to every S3 request, so this file is",
    "what makes the store usable at all -- it is not an optional hardening step."
  ],
  "identities": [
    {
      "name": "wwi",
      "credentials": [
        { "accessKey": "__ACCESS_KEY__", "secretKey": "__SECRET_KEY__" }
      ],
      "actions": ["Admin", "Read", "Write", "List", "Tagging"]
    }
  ]
}
